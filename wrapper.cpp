#include <vector>
#include <sys/time.h>
#include <unistd.h>
#include <mpi.h>

using namespace std;

namespace wrapper {
    void MPI_Send() {

        return;
    }

    void MPI_Recv() {

        return;
    }

    void MPI_Reduce() {

        return;
    }
}